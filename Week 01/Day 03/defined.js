// check if defined
function notReady(){
    
}

// check if defined
function totallyReady(){
    return "I'm so ready to be tested."
}

// should be defined if num is less than or equal to 100, otherwise undefined
function needSpecificInput(num){
    if(num <= 100){
        return "number accepted."
    }else{
        return undefined;
    }
}

